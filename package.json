{
  "name": "test-app",
  "version": "1.0.0",
  "description": "Minimal test app for debugging NixOS GitHub runner permissions",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {
    "uuid": "^13.0.0"
  }
}